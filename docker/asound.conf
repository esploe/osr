# No real sound card exists in the container. Point ALSA's default device
# at a null sink so anything that tries to open audio (danser-go opens an
# audio backend even in headless record mode) gets silence instead of an
# error.
pcm.!default {
    type null
}
ctl.!default {
    type null
}
